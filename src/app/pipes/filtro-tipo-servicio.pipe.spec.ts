import { FiltroTipoServicioPipe } from './filtro-tipo-servicio.pipe';

describe('FiltroTipoServicioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTipoServicioPipe();
    expect(pipe).toBeTruthy();
  });
});
