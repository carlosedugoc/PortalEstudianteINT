import { FiltroFaltantesPipe } from './filtro-faltantes.pipe';

describe('FiltroFaltantesPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFaltantesPipe();
    expect(pipe).toBeTruthy();
  });
});
