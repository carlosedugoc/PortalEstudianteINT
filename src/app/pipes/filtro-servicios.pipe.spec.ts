import { FiltroServiciosPipe } from './filtro-servicios.pipe';

describe('FiltroServiciosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroServiciosPipe();
    expect(pipe).toBeTruthy();
  });
});
